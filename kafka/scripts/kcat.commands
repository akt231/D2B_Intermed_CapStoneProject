#get topic name+partitions+replicas: run outside docker
docker run --rm -it --network=d2b_intermed_capstoneproject_appnet edenhill/kcat:1.7.1 -b broker:9092 -L

#test out producer
$ kcat -b broker:9093 -t test-topic -P  # producer
one line per message
another line

#test out consumer:read messages you have produced
kcat -b localhost:9093 -t test-topic -C  # consumer
one line per message
another line
% Reached end of topic test-topic [0] at offset 2



